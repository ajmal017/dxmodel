%legend Closed trades

%table#exited_trades
  = render partial: 'headers', locals: {include_exit: false}

  %tbody
    - @exited_trades.each do |trade|
      = render partial: trade, locals: {include_exit: false}

:javascript
  $(document).ready(function() {
    $('table').tablesorter({theme: 'blue'});
  })

