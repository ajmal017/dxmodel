%h1 FX Rates

%table.table.table-hover.table-condensed
  %thead
    %th Date
    - if CURRENCIES.includes?('SGD')
      %th USDSGD 
    - if CURRENCIES.includes?('HKD')
      %th USDHKD
    - if CURRENCIES.includes?('CNY')
      %th USDCNY
    - if CURRENCIES.includes?('GBP')
      %th USDGBP
    - if CURRENCIES.includes?('EUR')
      %th USDEUR
  %tbody
    - @fx_rates.each do |fx_rate|
      %tr
        %td= fx_rate.date
        - if CURRENCIES.includes?('SGD')
          %td= fx_rate.usdsgd
        - if CURRENCIES.includes?('HKD')
          %td= fx_rate.usdhkd
        - if CURRENCIES.includes?('CNY')
          %td= fx_rate.usdcny
        - if CURRENCIES.includes?('GBP')
          %td= fx_rate.usdgbp
        - if CURRENCIES.includes?('EUR')
          %td= fx_rate.usdeur
