%h1 Sign Up

= simple_form_for(@user, :defaults => {:required => false, :input_html => {:class => 'xlarge'}}) do |f|
  %fieldset
    = f.input :email
    = f.input :password
    = f.input :password_confirmation

  .control-group
    .controls
      = f.submit "Sign up", :class => 'btn btn-primary'
