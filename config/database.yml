base: &base
  adapter: mysql2
  username: root
  host: localhost
  encoding: utf8
  timeout: 5000

development:
  <<: *base
  database: dxmodel_dev

#test:
#  <<: *base
#  database: dxmodel_test

production:
  <<: *base
  database: dxmodel_prod
  socket: /var/run/mysqld/mysqld.sock #Ubuntu
